import { Spin } from 'antd';

const FullScreenSpin = () => {
    return (
        <div className="full-screen-spin-container">
            <Spin size="large" />
        </div>
    );
};

export default FullScreenSpin;